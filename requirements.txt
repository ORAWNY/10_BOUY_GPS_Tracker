# Core GUI
PyQt6

# Data
pandas
numpy

# Geospatial / mapping
geopandas
cartopy
shapely
pyproj
scipy

# Plotting
matplotlib

# Networking / web
requests
flask

# Authentication
PyJWT

# Windows-only (Outlook COM automation)
pywin32; sys_platform == "win32"

# Timezone data
tzdata

# Build tooling (only needed to produce .exe / .msi)
# cx_Freeze
